kind = "web"
previewPath = "/"
title = "MindForge"
version = "1.0.0"
id = "artifacts/mindforge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21527

[services.development]
run = "pnpm --filter @workspace/mindforge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mindforge", "run", "build" ]
publicDir = "artifacts/mindforge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21527"
BASE_PATH = "/"
